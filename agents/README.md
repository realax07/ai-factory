# Агенты конвейера — реестр, вход/выход, назначение

> Каждый файл в этой папке — **системный промпт** сабагента: подается целиком как системное сообщение при запуске на задаче. Здесь: кто эти агенты, что делают, зачем нужны, что принимают и отдают. Общий процесс — [../README.md](../README.md), оперативные правила — [../AGENTS.md](../AGENTS.md).

## Почему агентов именно столько

Одна роль = один изолированный контекст = одна ответственность. Автор не ревьюит себя; ревьюер не пишет код; аналитик требований не решает архитектуру. Изоляция дает три вещи: независимую валидацию (глаза без конфликта интересов), короткий чистый контекст (агент не тащит чужие детали), подменяемость (промпт + задача = любой запуск воспроизводим).

## Режимы запуска

- **plan** — только анализ и диалог, запись в репозиторий запрещена.
- **plan-write** — диалог + запись артефактов своей роли (без push). Для БА: черновик → подтверждение Заказчика → запись.
- **auto-edit** — запись в своей зоне разрешена; push main и чужие зоны запрещены (стандарт конвейера).
- **yolo** — полный доступ; в конвейере не используется (только песочницы).

## Таблица зон записи (норма для всех ролей)

| Агент | Режим | Зона записи (только она) | Push |
|---|---|---|---|
| Бизнес-аналитик | plan-write | `requirements.md`, `docs/ba/answers_roundN.md` | нет |
| Системный аналитик | auto-edit | `openspec/changes/<id>/**`, `sdd.md` | нет |
| Разработчик | auto-edit | файлы своей задачи, чекбокс в `tasks.md`, своя ветка `feature/*` | своей ветки — да; main — нет |
| Код-ревьюер | auto-edit | `code-reviews/<change-id>/review-*.md` | нет |
| QA: аналитик чеклистов | auto-edit | `test-model/checklists/` | нет |
| QA: автор кейсов | auto-edit | `test-model/new/<change-id>/` | нет |
| QA: ревьюер кейсов | auto-edit | `test-model/reviews/`, перенос в `test-model/approved/` | нет |
| QA: автоматизатор | auto-edit | `tests/**`, `test-model/bugs/`, dev-зависимости | нет |
| QA: impact-аналитик | auto-edit | `test-model/impact/<change-id>.md` | нет (роль создается при первом change с MODIFIED-дельтами, H5) |
| QA: регресс-аналитик | auto-edit | `test-model/regression/manifest.md` | нет (роль создается при первом релизе, G5) |
| Дизайнер UI | auto-edit | `design/**` проекта | нет (роль создается 2026-09-22 для R2/задачи 2.5, Д-5; мокапы и токены, продукт-код не пишет) |
| ПМ (оркестратор) | — | `PLAN.md`, `BACKLOG.md`, merge PR, push | да |

Запись вне зоны = нарушение флоу; отчет отклоняется ПМ.

## Агенты: назначение, вход/выход, зачем нужен

### 1. Бизнес-аналитик — [ba_agent.md](ba_agent.md)

**Зачем:** переводит слова Заказчика в проверяемые требования, не добавляя отсебятину. Единственный, кто говорит с Заказчиком (через ПМ) на этапе анализа. Лимит: максимум 2 раунда открытых вопросов, далее — дефолты «примем X, возразите».

- **Вход:** запрос Заказчика (тема, ответы ОВ-N дословно).
- **Выход:** `requirements.md` (7 разделов, FR-N/NFR-N, MoSCoW, преамбула статуса/автора/истории) + `docs/ba/answers_roundN.md` (дословные цитаты, связь ОВ-N → изменение) + `docs/product-overview.md` (мастер-док, обновление на каждом change).
- **Ключевые границы:** не проектирует архитектуру; не закрывает открытые вопросы сам; противоречия фиксирует, не разрешает.
- **Передает:** СА (только при статусе УТВЕРЖДЕН — решает Заказчик).

### 2. Системный аналитик — [sa_agent.md](sa_agent.md)

**Зачем:** превращает требования в формальный контракт поведения (спека OpenSpec) и план реализации (tasks.md). Здесь требования становятся тестируемыми: каждый Requirement получает Scenario GIVEN/WHEN/THEN.

- **Вход:** УТВЕРЖДЕННЫЙ `requirements.md` (+ answers-артефакты всех раундов).
- **Выход:** `openspec/changes/<id>/` (proposal, дельты specs, design, tasks) + `sdd.md` (стек, API-контракты, модель данных — служебные эндпоинты обязательны).
- **Ключевые границы:** спека описывает наблюдаемое поведение, не реализацию; не начинает без УТВЕРЖДЕНО; не трогает чужие change-пакеты.
- **Передает:** dev (задачи), qa_checklist (спека как источник чеклиста). После реализации — архивация дельт в master-spec (контракт 7).

### 3. Разработчик — [dev_agent.md](dev_agent.md)

**Зачем:** реализует ровно одну задачу tasks.md в изолированной сессии — узкий контекст = глубокая концентрация + дешевый откат.

- **Вход:** задача из tasks.md (текст + ссылки на спеку/дизайн), ветка `feature/<change-id>-<NN>` от main.
- **Выход:** код в ветке + PR (gh) + чекбокс задачи. Ревьюеру — процедура проверки в ТЗ.
- **Ключевые границы:** спека — арбитр (расхождение = эскалация, не правка); не реализует вне tasks.md; не пишет QA-артефакты; push main запрещен.
- **Принимает:** вердикт code_reviewer (approve → merge ПМ; return → доработка в той же ветке).
- **Смежная зона:** изменения пайплайна (flow_check, flow.yml, scripts/) — тоже его зона (G7: DevOps-агента нет).

### 4. Код-ревьюер — [code_reviewer_agent.md](code_reviewer_agent.md)

**Зачем:** независимая валидация кода: три круга (спека как закон → best practices → интеграция) + регресс-прогон вместо своих смоков. Главный фильтр качества: ловит то, что автор не видит (гонка 5.1, дыры fast line — найдены именно здесь).

- **Вход:** PR диффа + спека задачи + процедура проверки из ТЗ.
- **Выход:** вердикт approve/return + таблица замечаний (blocker/major/minor + рекомендации) в `code-reviews/`.
- **Ключевые границы:** не правит код сам; «я бы написал иначе» — не замечание; documentation в диффе проверяется буквальным воспроизведением.

### 5. QA: аналитик чеклистов — [qa_checklist_agent.md](qa_checklist_agent.md)

**Зачем:** ревертит спеку в плоский проверяемый список (CHK-N) с техниками граничных значений, негативных классов и состояний. Спека говорит «что должно»; чеклист говорит «что и как проверяем». Нетестируемое место спеки = эскалация (дефекты спеки — раздел в чеклисте).

- **Вход:** спека change-пакета (дельты или master-specs).
- **Выход:** `test-model/checklists/<id>.md` — CHK-N с источником (Requirement + FR-N), типом, приоритетом; программная проверка покрытия (каждый Requirement ≥1 пункта).

### 6. QA: автор тест-кейсов — [qa_case_author_agent.md](qa_case_author_agent.md)

**Зачем:** развертывает каждый CHK в исполняемый кейс: предусловия, атомарные шаги, проверяемое ожидание (код + тело/состояние), конкретные данные. Здесь рождается точность, по которой потом пишутся тесты и судятся падения.

- **Вход:** чеклист (CHK-N) + спека как арбитр.
- **Выход:** `test-model/new/<id>/` — TC-N (1 кейс = 1 CHK; формат «1 кейс = 1 файл» — H1, при внедрении).
- **Ключевые границы:** не меняет чеклист; не объединяет/дробит пункты; DS-пункты (неспецифицированное поведение) фиксируют факт, не ожидание.

### 7. QA: ревьюер тест-кейсов — [qa_case_reviewer_agent.md](qa_case_reviewer_agent.md)

**Зачем:** независимая проверка кейсов (6 осей: трассировка, спека, воспроизводимость, проверяемость, данные, негатив) + ревью кода автотестов (G8: ассерты дословны кейсу, изоляция, детерминированность, селекторы, TC-ID). Только он переносит кейсы в approved/ — единственный источник законных автотестов.

- **Вход:** кейсы в `new/` + чеклист + спека (+ код тестов на G8-итерации).
- **Выход:** `test-model/reviews/<id>/review-NNN.md` (append-only итерации) + вердикт; при approve — перенос в `test-model/approved/<id>/`.
- **Ключевые границы:** не правит кейсы сам; не одобряет с blocker/major; спор = спека, не мнение.

### 8. QA: автоматизатор — [qa_automation_agent.md](qa_automation_agent.md)

**Зачем:** превращает approved-кейсы в постоянный регрессионный набор — то, что гоняется перед каждым релизом и защищает от регрессий навсегда. Падение теста на корректном коде = баг-репорт, а не правка теста.

- **Вход:** только approved-кейсы (контракт 6).
- **Выход:** `tests/api/` (pytest+requests), `tests/web/` (Playwright) — селекторы дословно из кейсов, автожидания, sleep=0, TC-ID в docstring, метка `# regression: keep|candidate-archive` (G5); баг-репорты в `test-model/bugs/BUG-NNN`.
- **Ключевые границы:** не трогает код продукта; не подгоняет тест под сломанное поведение.

### 9. QA: impact-аналитик — (промпт создается при первом change с MODIFIED-дельтами, H5)

**Зачем:** держит регресс актуальным при изменении поведения. Когда СА готовит MODIFIED/REMOVED дельты, часть существующих тестов устаревает: они тестируют старое поведение. Impact-аналитик до qa_checklist сверяет дельты с существующим набором и выносит вердикт по каждому затронутому тесту — иначе устаревший тест стреляет ложным пожаром на релизном прогоне.

- **Вход:** дельты change-пакета (MODIFIED/REMOVED Requirements) + манифест регресса + трассировка TC ↔ Requirement.
- **Выход:** `test-model/impact/<change-id>.md` — вердикт по каждому затронутому тесту: **keep** (не затронут) / **revalidate** (поведение изменилось → тест помечается `pending_update`, исключается из релизного прогона, кейс обновляется в чеклисте) / **retire** (тестировал удаленное → переносится в архив).
- **Ключевые границы:** не правит тесты сам (только вердикты); семантику «действительно ли изменилось поведение» сверяет со спекой, не с кодом.
- **Передает:** qa_checklist (требования «обновить кейс X» попадают в чеклист), qa_automation (pending_update-метки).

### 10. QA: регресс-аналитик — (промпт создается при первом релизе, G5)

**Зачем:** пост-релизно решает судьбу каждого теста: keep (устойчивое поведение — гоняется всегда) или archive (разовое — миграция, тест конкретного бага). Держит регресс компактным и быстрым на дистанции.

- **Вход:** метки `regression: keep|candidate-archive` + обоснования из тестов + спека.
- **Выход:** `test-model/regression/manifest.md` — тест → решение + причина.
- **Ключевые границы:** работает по меткам и оглавлению, не по всему коду; архивируемые тесты переносятся в `tests/archive/` (не удаляются).

### 11. ПМ (оркестратор) — эта сессия

**Зачем:** единственный владелец общей картины и контакт Заказчика. Собирает goal по шаблону (templates/task_delegation.md), приемка по чеклисту (ворота / зона / артефакты / история / коммит), merge PR, пуш, ведение PLAN.md (журнал делегаций) и BACKLOG.md (ретро-уроки). Контролирует здоровье сабагентов (пинг транскриптов на деградацию).

## Поток конвейера

```
Заказчик ⇄ [БА | plan-write] → requirements.md → [СА | auto-edit] → change-пакет + sdd.md
                                                                        │
      ┌─────────────────────────────────────────────────────────────────┘
      ▼  цикл на каждую задачу (ветка feature/* → PR)
[dev] → PR → [code_reviewer] → approve → ПМ merge | return → доработка
      │
      ▼  после всех задач
[qa_checklist] → [qa_case_author] ⇄ [qa_case_reviewer] → approved/
      │
      ▼
[qa_automation] → tests/ → [qa_case_reviewer] ревью кода (G8)
      │
      ▼  релиз (G9: feature → release-candidate → main)
ПМ: предрелизный регресс + чистка промежуточных → контракт 7 (архивация)
      │
      ▼  пост-релизно (G5)
[qa_regression_analyst] → манифест keep/archive
```

## Связанные документы

- Полный процесс (зачем, гарантии, артефакты): [../README.md](../README.md)
- Контракты этапов (вход/выход формально): [../contracts/artifact_contract.md](../contracts/artifact_contract.md)
- Скиллы (процедуры best practices): [../skills/](../skills/)
- Шаблон делегации задачи: [../templates/task_delegation.md](../templates/task_delegation.md)
- Спеки конвейера: [../openspec/specs/](../openspec/specs/)
- Бэклог тюнинга: [../BACKLOG.md](../BACKLOG.md)

## Планируемые роли

- **qa_regression_analyst** — промпт создается при первом релизе (G5): пост-релизный анализ меток keep/archive, манифест регресса.
- **Release-инженер** — рассмотрен и отклонен (G7): деплой-обязанности у dev/ПМ, depлоy-инструкции проверяются воспроизведением.
